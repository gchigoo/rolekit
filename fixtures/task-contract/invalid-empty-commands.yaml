schema: rolekit/task-contract@1
id: RK-20260728-003
kind: implementation
role: implementer
executor: pi-default
objective: Structurally valid but acceptance.commands is empty
context:
  required_files:
    - packages/core/src/index.ts
  docs: []
scope:
  writable:
    - packages/core/**
  forbidden:
    - packages/cli/**
constraints: []
deliverables:
  - nothing meaningful without acceptance commands
acceptance:
  commands: []
  assertions:
    - must fail semantic layer
execution:
  worktree: isolated
  max_tool_calls: 20
  network: deny
  timeout_minutes: 10
escalation:
  on_scope_change: return_blocked
  on_new_dependency: require_approval
  on_ambiguous_requirement: return_question
